# ==========================================================================
# Research Droplet Environment
# ==========================================================================

# MongoDB — point to production droplet's VPC private IP
MONGO_URI=mongodb://10.x.x.x:27017
MONGO_DB=bluehorseshoe

# Tiingo API — needed for -b backfill if ever used on research box
TIINGO_API_KEY=
TIINGO_CPS=5
